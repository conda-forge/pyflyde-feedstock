{% set name = "pyflyde" %}
{% set version = "0.0.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyflyde-{{ version }}.tar.gz
  sha256: 9adcd9c3ff725cee6cb7de8efb24b4a35233c296c476e9d33d69438eebd29648

build:
  entry_points:
    - pyflyde = flyde.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >=3.9
    - pyyaml

test:
  imports:
    - flyde
  commands:
    - pip check
    - pyflyde --help
  requires:
    - pip

about:
  summary: Python SDK and runtime for Flyde - a visual flow-based programming language and IDE.
  home: https://pyflyde.readthedocs.io
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
