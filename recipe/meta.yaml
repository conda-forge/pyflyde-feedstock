{% set name = "pyflyde" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyflyde-{{ version }}.tar.gz
  sha256: 085688fe548c173690f1ee4426442eaf2259b70434e43bc1256271c8acb02957

build:
  entry_points:
    - pyflyde = flyde.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >={{ python_min }}
    - pyyaml

test:
  imports:
    - flyde
  commands:
    - pip check
    - pyflyde --help
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: Python SDK and runtime for Flyde - a visual flow-based programming language and IDE.
  home: https://pyflyde.readthedocs.io
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
